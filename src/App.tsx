import { HeroSection } from './components/v0/hero-section';

function App() {
  return (
    // Adicionamos a classe 'dark' manualmente aqui para garantir o estilo
    <div className="dark">
      <HeroSection />
    </div>
  );
}

export default App;
